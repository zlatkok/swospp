Description of build process, necessary tools, directory hierarchy, what goes where...

Programs needed for full recompilation:
- GNU make
- any working Unix environment (for shell, and some UNIX utilities) - I'm using DJGPP + MINGW make
- Open Watcom C Compiler
- NASM 0.98.36 my patched version (because they broke some macros... see if we can fix this)
- Perl (any not-totally-ancient version will do)
- alink, my modified version
- go32, my util [OBSOLETE - not needed with the new Perl build script]
- winzip, for dist package packing [not used atm, see if we can use it again or remove it]

bin    - binary files, SWOS++, loader, utility programs...
dist   - this is where the dist archive will be created
doc    - various documentation, both internal and published
etc    - various files that don't belong anywhere else, listings, lib files, etc.
loader - loader source
main   - SWOS++ source
mapcvt - source of converter from map file to .asm/.inc/.h
obj    - object files, as well as executables
other  - other sources not belonging to the project
patch  - patcher source
pe2bin - source of converter from Win32 PE to my binary format
test   - unit tests for various components, and misc test code

Includes

Svaki c fajl mora ukljuciti swos.h, i pozeljno util.h, tim redosledom.
Za svaki fajl koji zavisi od swos.h mora se dodati i swossym.h kao dependency.
[OBSOLETE - new Perl script automatically determines dependencies]
#pragma once konvencija

Defines

DEBUG (release = !debug)
TEST
jos nesto?

Perl Makefile

mapcvt, detaljni opis, kako se dodaju simboli

Describe, TODO :)

Proces sastavljanja bin fajla

Najpre se kompajliraju i asembluju osnovni fajlovi projekta. Dobija se win32 pe
executable, pomalo specifican, sa odredjenim potrebnim sekcijama i
eksportovanim simbolima. Zatim se to salje konvertoru koji konvertuje exe u
binarni fajl koji SWOS++ loader moze da ucita i startuje.
Iz map fajla se kreira master include fajl koji koriste gotovo svi asm fajlovi.